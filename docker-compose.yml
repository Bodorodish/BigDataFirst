version: '3.8'
services:
  db:
    image: postgres:15
    container_name: pet_shop_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: pet_shop_analytics
    ports:
      - "5433:5432" # у меня занят 5432, поэтому 5433